{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            "port": 9004,
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}",
                "/var/www/html/wp-content/themes/cargo": "${workspaceFolder}/cargo"
            }
        }
    ]
}